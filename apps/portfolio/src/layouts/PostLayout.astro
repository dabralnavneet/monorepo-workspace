---
import Layout from './Layout.astro';

const { frontmatter } = Astro.props;
---

<Layout title={`${frontmatter.title} | Navneet Dabral`}>
  <main class="w-full max-w-2xl mx-auto px-6 py-24 md:py-32 min-h-screen">
    <a
      href="/"
      class="inline-flex items-center gap-2 text-zinc-600 font-mono text-xs uppercase tracking-[0.2em] hover:text-zinc-300 transition-colors duration-200 mb-14"
    >
      ← back
    </a>

    <article>
      <header class="mb-14">
        <div class="flex items-center gap-4 text-zinc-600 font-mono text-xs uppercase tracking-[0.2em] mb-6">
          <span>{frontmatter.date}</span>
          <span class="w-1 h-1 rounded-full bg-zinc-700 inline-block"></span>
          <span>{frontmatter.readTime ?? '5 min read'}</span>
        </div>
        <h1 class="text-3xl md:text-5xl font-semibold text-white tracking-tight leading-tight">
          {frontmatter.title}
        </h1>
      </header>

      <div class="prose-dark">
        <slot />
      </div>
    </article>
  </main>
</Layout>

<style is:global>
  .prose-dark p {
    color: #a1a1aa;
    margin-bottom: 1.75rem;
    line-height: 1.85;
    font-size: 1rem;
  }
  .prose-dark h2 {
    color: #e4e4e7;
    font-size: 1.5rem;
    font-weight: 600;
    margin-top: 3rem;
    margin-bottom: 1.25rem;
    letter-spacing: -0.02em;
    line-height: 1.3;
  }
  .prose-dark h3 {
    color: #d4d4d8;
    font-size: 1.15rem;
    font-weight: 500;
    margin-top: 2rem;
    margin-bottom: 1rem;
  }
  .prose-dark a {
    color: #e4e4e7;
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: #3f3f46;
    transition: text-decoration-color 0.2s;
  }
  .prose-dark a:hover {
    text-decoration-color: #a1a1aa;
  }
  .prose-dark strong {
    color: #e4e4e7;
    font-weight: 500;
  }
  .prose-dark em {
    color: #a1a1aa;
    font-style: italic;
  }
  .prose-dark ul,
  .prose-dark ol {
    padding-left: 1.5rem;
    margin-bottom: 1.75rem;
    color: #a1a1aa;
  }
  .prose-dark li {
    margin-bottom: 0.5rem;
    line-height: 1.7;
  }
  .prose-dark blockquote {
    border-left: 2px solid #27272a;
    padding-left: 1.25rem;
    margin: 2rem 0;
    color: #71717a;
    font-style: italic;
  }
  .prose-dark code {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.85em;
    color: #d4d4d8;
    background: #111;
    padding: 0.15em 0.4em;
    border-radius: 3px;
  }
  .prose-dark hr {
    border-color: #18181b;
    margin: 3rem 0;
  }
</style>
